# Anthropic Claude (required for chat) — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# Stripe (required for Buy Now — use test keys in development)
STRIPE_SECRET_KEY=sk_test_...

# Public URL for Stripe redirect (no trailing slash)
NEXT_PUBLIC_APP_URL=http://localhost:3000
